import { useState } from 'react'
import { CameraAltOutlined, KeyOutlined, LockOutlined, SecurityOutlined } from '@mui/icons-material'
import { Alert, Avatar, Box, Button, Card, CardContent, Dialog, DialogActions, DialogContent, DialogTitle, Grid, Stack, Switch, TextField, Typography } from '@mui/material'
import api, { errorMessage } from '../api/client'
import { PageIntro, initials } from '../components/Common'
import { useAuth } from '../context/AuthContext'

function PasswordDialog({ open, onClose }) {
  const [form, setForm] = useState({ current_password: '', new_password: '', confirm: '' }); const [status, setStatus] = useState({ error: '', success: '' }); const [busy, setBusy] = useState(false)
  const submit = async () => { if (form.new_password !== form.confirm) return setStatus({ error: 'New passwords do not match.', success: '' }); setBusy(true); try { const { data } = await api.post('/auth/change-password/', form); setStatus({ success: data.detail, error: '' }); setForm({ current_password: '', new_password: '', confirm: '' }) } catch (err) { setStatus({ error: errorMessage(err), success: '' }) } finally { setBusy(false) } }
  return <Dialog open={open} onClose={onClose} fullWidth maxWidth="xs"><DialogTitle>Change password</DialogTitle><DialogContent><Stack spacing={2} pt={1}>{status.error && <Alert severity="error">{status.error}</Alert>}{status.success && <Alert severity="success">{status.success}</Alert>}<TextField label="Current password" type="password" value={form.current_password} onChange={(e) => setForm({ ...form, current_password: e.target.value })} /><TextField label="New password" type="password" value={form.new_password} onChange={(e) => setForm({ ...form, new_password: e.target.value })} /><TextField label="Confirm new password" type="password" value={form.confirm} onChange={(e) => setForm({ ...form, confirm: e.target.value })} /></Stack></DialogContent><DialogActions><Button onClick={onClose}>Close</Button><Button variant="contained" onClick={submit} disabled={busy || !form.current_password || !form.new_password}>Change password</Button></DialogActions></Dialog>
}

function TwoFactorDialog({ open, onClose, onEnabled }) {
  const [setup, setSetup] = useState(null); const [code, setCode] = useState(''); const [error, setError] = useState('')
  const load = async () => { if (!setup) { try { const { data } = await api.get('/auth/2fa/setup/'); setSetup(data) } catch (err) { setError(errorMessage(err)) } } }
  if (open && !setup && !error) load()
  const verify = async () => { try { await api.post('/auth/2fa/verify/', { code }); onEnabled(); onClose() } catch (err) { setError(errorMessage(err)) } }
  return <Dialog open={open} onClose={onClose} fullWidth maxWidth="xs"><DialogTitle>Set up two-factor authentication</DialogTitle><DialogContent><Typography color="text.secondary" mb={2}>Scan this code with an authenticator app, then enter its six-digit code.</Typography>{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}{setup && <Stack alignItems="center" spacing={2}><Box component="img" src={setup.qr_code} alt="Authenticator QR code" width={210} height={210} /><Typography variant="caption" color="text.secondary" sx={{ wordBreak: 'break-all' }}>Manual key: {setup.secret}</Typography><TextField label="Verification code" inputProps={{ inputMode: 'numeric', maxLength: 6 }} value={code} onChange={(e) => setCode(e.target.value.replace(/\D/g, ''))} fullWidth /></Stack>}</DialogContent><DialogActions><Button onClick={onClose}>Cancel</Button><Button variant="contained" disabled={code.length !== 6} onClick={verify}>Enable 2FA</Button></DialogActions></Dialog>
}

export default function ProfilePage() {
  const { user, setUser, refreshProfile } = useAuth(); const [form, setForm] = useState({ first_name: user.first_name, last_name: user.last_name, email: user.email, department: user.department, position: user.position, phone: user.phone, bio: user.bio }); const [avatar, setAvatar] = useState(null); const [status, setStatus] = useState({ error: '', success: '' }); const [passwordOpen, setPasswordOpen] = useState(false); const [twoFactorOpen, setTwoFactorOpen] = useState(false)
  const save = async () => { const body = new FormData(); Object.entries(form).forEach(([key, value]) => body.append(key, value || '')); if (avatar) body.append('avatar', avatar); try { const { data } = await api.patch('/auth/profile/', body); setUser(data); localStorage.setItem('user', JSON.stringify(data)); setStatus({ success: 'Profile updated successfully.', error: '' }) } catch (err) { setStatus({ error: errorMessage(err), success: '' }) } }
  return <><PageIntro eyebrow="Your account" title="Profile & security" subtitle="Keep your faculty information and sign-in protections current." />{status.error && <Alert severity="error" sx={{ mb: 2 }}>{status.error}</Alert>}{status.success && <Alert severity="success" sx={{ mb: 2 }}>{status.success}</Alert>}<Grid container spacing={2.5}><Grid size={{ xs: 12, lg: 8 }}><Card><CardContent><Stack direction={{ xs: 'column', sm: 'row' }} gap={2.5} alignItems={{ sm: 'center' }} mb={3}><Box position="relative"><Avatar src={avatar ? URL.createObjectURL(avatar) : user.avatar_url} sx={{ width: 88, height: 88, bgcolor: 'primary.main', fontSize: 28 }}>{initials(user)}</Avatar><Button component="label" variant="contained" size="small" sx={{ position: 'absolute', minWidth: 32, width: 32, height: 32, p: 0, right: -4, bottom: 0, borderRadius: '50%' }}><CameraAltOutlined fontSize="small" /><input hidden type="file" accept="image/*" onChange={(e) => setAvatar(e.target.files[0])} /></Button></Box><Box><Typography variant="h6" fontWeight={750}>{user.full_name}</Typography><Typography color="text.secondary">{user.role.replaceAll('_', ' ')} · {user.employee_id}</Typography></Box></Stack><Grid container spacing={2}><Grid size={{ xs: 12, sm: 6 }}><TextField label="First name" fullWidth value={form.first_name} onChange={(e) => setForm({ ...form, first_name: e.target.value })} /></Grid><Grid size={{ xs: 12, sm: 6 }}><TextField label="Last name" fullWidth value={form.last_name} onChange={(e) => setForm({ ...form, last_name: e.target.value })} /></Grid><Grid size={{ xs: 12, sm: 6 }}><TextField label="Email" fullWidth value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></Grid><Grid size={{ xs: 12, sm: 6 }}><TextField label="Phone" fullWidth value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} /></Grid><Grid size={{ xs: 12, sm: 6 }}><TextField label="Department" fullWidth value={form.department} onChange={(e) => setForm({ ...form, department: e.target.value })} /></Grid><Grid size={{ xs: 12, sm: 6 }}><TextField label="Position" fullWidth value={form.position} onChange={(e) => setForm({ ...form, position: e.target.value })} /></Grid><Grid size={12}><TextField label="About" multiline minRows={3} fullWidth value={form.bio} onChange={(e) => setForm({ ...form, bio: e.target.value })} /></Grid></Grid><Button variant="contained" sx={{ mt: 2.5 }} onClick={save}>Save profile</Button></CardContent></Card></Grid><Grid size={{ xs: 12, lg: 4 }}><Stack spacing={2.5}><Card><CardContent><Stack direction="row" gap={1.5} alignItems="center" mb={2}><Box width={42} height={42} borderRadius={2.5} bgcolor="primary.light" color="primary.dark" display="grid" sx={{ placeItems: 'center' }}><LockOutlined /></Box><Box><Typography fontWeight={750}>Password</Typography><Typography variant="caption" color="text.secondary">Email confirmation included</Typography></Box></Stack><Button variant="outlined" fullWidth startIcon={<KeyOutlined />} onClick={() => setPasswordOpen(true)}>Change password</Button></CardContent></Card><Card><CardContent><Stack direction="row" gap={1.5} alignItems="center"><Box width={42} height={42} borderRadius={2.5} bgcolor="primary.light" color="primary.dark" display="grid" sx={{ placeItems: 'center' }}><SecurityOutlined /></Box><Box flex={1}><Typography fontWeight={750}>Two-factor authentication</Typography><Typography variant="caption" color="text.secondary">{user.two_factor_enabled ? 'Your account is protected' : 'Add an extra sign-in step'}</Typography></Box><Switch checked={user.two_factor_enabled} onChange={() => !user.two_factor_enabled && setTwoFactorOpen(true)} /></Stack></CardContent></Card></Stack></Grid></Grid><PasswordDialog open={passwordOpen} onClose={() => setPasswordOpen(false)} /><TwoFactorDialog open={twoFactorOpen} onClose={() => setTwoFactorOpen(false)} onEnabled={refreshProfile} /></>
}

